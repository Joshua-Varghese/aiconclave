name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: pages-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Production deploy
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Static assets only in publish/. D1 + Pages Functions live under db/
      # and are picked up via --config db/wrangler.toml.
      - name: Prepare publish directory
        run: |
          mkdir -p publish
          cp index.html style.css script.js publish/
          cp -r About Schedule Participate Register publish/

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4.38.0"
          command: pages deploy publish --project-name=aiconclave --branch=main --commit-dirty=true --config db/wrangler.toml
